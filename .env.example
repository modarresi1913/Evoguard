# EvoGuard MVP - Environment Variables

# GitHub Webhook Secret (set in GitHub repo settings > Webhooks)
GITHUB_WEBHOOK_SECRET=

# GitHub Personal Access Token (needs repo:status and public_repo scopes)
GITHUB_TOKEN=

# Database URL (SQLite for MVP)
DATABASE_URL="file:./dev.db"
